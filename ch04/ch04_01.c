#include <stdio.h>

// inefficient way to calculate the 8th triangular number
int main(void)
{
    int triangularNumber;

    triangularNumber = 1 + 2 + 3 + 4 + 5 + 6 + 7 + 8;

    printf("The eight triangular number is %i\n", triangularNumber);

    return 0;
}
